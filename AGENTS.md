# License Cost Sweep Guidance

Finds wasted licence spend, including disabled accounts with paid licences, shared mailboxes with unnecessary licences, and licensed users that have never logged in.

Durable project guidance lives in `CLAUDE.md`; repository skills are in `.claude/skills/`. This file is a pointer for any agent tooling that reads `AGENTS.md` rather than `CLAUDE.md` — `CLAUDE.md` is the canonical, up-to-date source.
